<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Applojadebolos"
        tools:targetApi="31">
        <activity
            android:name=".login.presentation.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".register.presentation.RegisterActivity"
            android:exported="true"></activity>

        <activity
            android:name=".recover_password.presentation.RecoverPasswordActivity"
            android:exported="true"></activity>

        <activity android:name=".home.presentation.HomeActivity"
            android:exported="true"></activity>

        <activity android:name=".cake_category_list.presentation.CakeCategoryListActivity"
            android:exported="true"></activity>

        <activity android:name=".cake_list.presentation.CakeListActivity"
            android:exported="true"></activity>

        <activity android:name=".cake_details.presentation.CakeDetailsActivity"
            android:exported="true"></activity>

       <activity android:name=".shopping_list.presentation.ShoppingListActivity"
           android:exported="true"></activity>
    </application>

</manifest>